function calcularAluguel(dias = 0) { // função
    const valorPorDia = 250;          // valor do dia
    console.log("Total a pagar: R$ " + (dias * valorPorDia)); 
}
calcularAluguel(4); // Deve imprimir "Total a pagar: R$ 1250"
